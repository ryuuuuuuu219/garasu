# Unity Project

このフォルダーは Unity プロジェクトです。

## 開き方

1. Unity Hub の「追加」または「Add project from disk」を選択します。
2. このフォルダー（`C:\なにか`）を指定します。
3. Unity Editor `6000.4.6f1` で開きます。

初回起動時に Unity が `Library`、追加の `ProjectSettings`、`.meta` ファイルなどを自動生成します。

